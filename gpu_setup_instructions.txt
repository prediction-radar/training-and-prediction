sudo apt-get install vim

ssh-keygen -t rsa

git clone git@github.com:trevorwiebe/radar-training.git

yes | python3 -m pip install tensorflow[and-cuda]

yes | pip3 install pandas matplotlib scikit-learn imageio